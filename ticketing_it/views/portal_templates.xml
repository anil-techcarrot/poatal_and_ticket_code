<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add IT Tickets to Portal Homepage -->
    <template id="portal_my_home_it_tickets"
              name="Portal My Home: IT Tickets Entry"
              inherit_id="portal.portal_my_home"
              priority="40">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <a href="/my/tickets" class="col-12 col-md-6 o_portal_docs_entry o_doc_button">
                <div class="o_doc_card">
                    <div class="o_doc_card_icon">
                        <i class="fa fa-ticket fa-3x text-primary"/>
                    </div>
                    <div class="o_doc_card_desc">
                        <span class="o_doc_card_title">IT Tickets</span>
                        <span class="o_doc_card_count">
                            <t t-if="ticket_count is defined">
                                <t t-esc="ticket_count"/>
                            </t>
                            <t t-else="">0</t>
                        </span>
                        <span class="o_doc_card_descr">Create and track your IT support tickets</span>
                    </div>
                </div>
            </a>
        </xpath>
    </template>

    <!-- Ticket List Page -->
    <template id="portal_my_tickets" name="My IT Tickets">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">IT Tickets</t>
            </t>
            <div class="container mt-3">
                <div class="row">
                    <div class="col-12">
                        <a href="/my/tickets/new" class="btn btn-primary mb-3">
                            <i class="fa fa-plus"/>
                            Create New Ticket
                        </a>
                    </div>
                </div>
                <t t-if="not tickets">
                    <div class="alert alert-info">
                        No tickets found. Create your first ticket!
                    </div>
                </t>
                <t t-if="tickets">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Ticket #</th>
                                <th>Subject</th>
                                <th>Type</th>
                                <th>Priority</th>
                                <th>Status</th>
                                <th>Created</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="tickets" t-as="ticket">
                                <tr>
                                    <td>
                                        <a t-attf-href="/my/tickets/#{ticket.id}">
                                            <t t-esc="ticket.name"/>
                                        </a>
                                    </td>
                                    <td>
                                        <t t-esc="ticket.subject"/>
                                    </td>
                                    <td>
                                        <t t-esc="dict(ticket._fields['ticket_type_id'].selection).get(ticket.ticket_type_id)"/>
                                    </td>
                                    <td>
                                        <t t-if="ticket.priority == '3'">
                                            <span class="badge badge-danger">Urgent</span>
                                        </t>
                                        <t t-elif="ticket.priority == '2'">
                                            <span class="badge badge-warning">High</span>
                                        </t>
                                        <t t-elif="ticket.priority == '1'">
                                            <span class="badge badge-info">Normal</span>
                                        </t>
                                        <t t-else="">
                                            <span class="badge badge-secondary">Low</span>
                                        </t>
                                    </td>
                                    <td>
                                        <span t-attf-class="badge badge-{{
                                            'success' if ticket.state == 'done' else
                                            'danger' if ticket.state == 'rejected' else
                                            'warning' if ticket.state in ['manager_approval', 'it_approval'] else
                                            'info'
                                        }}">
                                            <t t-esc="dict(ticket._fields['state'].selection).get(ticket.state)"/>
                                        </span>
                                    </td>
                                    <td>
                                        <t t-esc="ticket.create_date.strftime('%Y-%m-%d')"/>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    <div t-if="pager" class="o_portal_pager text-center">
                        <t t-call="portal.pager"/>
                    </div>
                </t>
            </div>
        </t>
    </template>

    <!-- Create Ticket Form -->
    <template id="portal_create_ticket_form" name="Create IT Ticket">
        <t t-call="portal.portal_layout">
            <div class="container mt-3">
                <h2>Create New IT Ticket</h2>

                <!-- Error message if submission failed -->
                <t t-if="error">
                    <div class="alert alert-danger">
                        <strong>Error:</strong>
                        Failed to create ticket. Please try again.
                    </div>
                </t>

                <!-- FIX: Line Manager info card shown before the form -->
                <div class="alert alert-info d-flex align-items-center mb-4" role="alert">
                    <i class="fa fa-user-circle fa-2x me-3 mr-3 text-primary"/>
                    <div>
                        <strong>Your Line Manager:</strong>
                        <t t-if="line_manager">
                            <span class="ms-2 ml-2 fw-bold">
                                <t t-esc="line_manager.name"/>
                            </span>
                            <small class="text-muted ms-2 ml-2">
                                — This ticket will be sent to your line manager for approval first.
                            </small>
                        </t>
                        <t t-else="">
                            <span class="ms-2 ml-2 text-warning">
                                <i class="fa fa-exclamation-triangle"/>
                                No line manager assigned.
                                Please contact your administrator before submitting a ticket.
                            </span>
                        </t>
                    </div>
                </div>

                <form action="/my/tickets/submit" method="POST" class="mt-2">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                    <div class="form-group">
                        <label for="ticket_type_id">Ticket Type *</label>
                        <select name="ticket_type_id" class="form-control" required="required">
                            <option value="">-- Select Type --</option>
                            <option value="hardware">Hardware Issue</option>
                            <option value="software">Software Issue</option>
                            <option value="social_media">Social Media Access</option>
                            <option value="network">Network Issue</option>
                            <option value="other">Other</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="priority">Priority *</label>
                        <select name="priority" class="form-control" required="required">
                            <option value="1" selected="selected">Normal</option>
                            <option value="2">High</option>
                            <option value="3">Urgent</option>
                            <option value="0">Low</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="subject">Subject *</label>
                        <input type="text" name="subject" class="form-control" required="required"
                               placeholder="Brief description of the issue"/>
                    </div>

                    <div class="form-group">
                        <label for="description">Description *</label>
                        <textarea name="description" class="form-control" rows="5" required="required"
                                  placeholder="Detailed description of the issue"></textarea>
                    </div>

                    <div class="form-group">
                        <label for="required_date">Required By Date (Optional)</label>
                        <input type="date"
                               name="required_date"
                               class="form-control"
                               t-att-min="datetime.date.today().strftime('%Y-%m-%d')"/>
                    </div>

                    <div class="form-group">
                        <!-- Disable submit if no line manager so the ticket won't get stuck -->
                        <t t-if="line_manager">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-paper-plane"/>
                                Submit Ticket
                            </button>
                        </t>
                        <t t-else="">
                            <button type="submit" class="btn btn-primary" disabled="disabled"
                                    title="Cannot submit: no line manager assigned">
                                <i class="fa fa-paper-plane"/>
                                Submit Ticket
                            </button>
                        </t>
                        <a href="/my/tickets" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </t>
    </template>

    <!-- Ticket Detail Page -->
    <template id="portal_ticket_detail" name="Ticket Detail">
        <t t-call="portal.portal_layout">
            <div class="container mt-3">
                <div class="row">
                    <div class="col-12">
                        <a href="/my/tickets" class="btn btn-secondary mb-3">
                            <i class="fa fa-arrow-left"/>
                            Back to Tickets
                        </a>
                    </div>
                </div>
                <div class="card">
                    <div class="card-header">
                        <h3>
                            <t t-esc="ticket.name"/>
                            -
                            <t t-esc="ticket.subject"/>
                        </h3>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <p>
                                    <strong>Type:</strong>
                                    <t t-esc="dict(ticket._fields['ticket_type_id'].selection).get(ticket.ticket_type_id)"/>
                                </p>
                                <p>
                                    <strong>Priority:</strong>
                                    <t t-if="ticket.priority == '3'">
                                        <span class="badge badge-danger">Urgent</span>
                                    </t>
                                    <t t-elif="ticket.priority == '2'">
                                        <span class="badge badge-warning">High</span>
                                    </t>
                                    <t t-elif="ticket.priority == '1'">
                                        <span class="badge badge-info">Normal</span>
                                    </t>
                                    <t t-else="">
                                        <span class="badge badge-secondary">Low</span>
                                    </t>
                                </p>
                                <p>
                                    <strong>Status:</strong>
                                    <span t-attf-class="badge badge-{{
                                        'success' if ticket.state == 'done' else
                                        'danger' if ticket.state == 'rejected' else
                                        'warning' if ticket.state in ['manager_approval', 'it_approval'] else
                                        'info'
                                    }}">
                                        <t t-esc="dict(ticket._fields['state'].selection).get(ticket.state)"/>
                                    </span>
                                </p>
                            </div>
                            <div class="col-md-6">
                                <p>
                                    <strong>Created:</strong>
                                    <t t-esc="ticket.create_date.strftime('%Y-%m-%d %H:%M')"/>
                                </p>
                                <p t-if="ticket.required_date">
                                    <strong>Required By:</strong>
                                    <t t-esc="ticket.required_date"/>
                                </p>
                                <p t-if="ticket.line_manager_id">
                                    <strong>Line Manager:</strong>
                                    <t t-esc="ticket.line_manager_id.name"/>
                                </p>
                            </div>
                        </div>
                        <hr/>
                        <h5>Description:</h5>
                        <div t-field="ticket.description"/>
                        <t t-if="ticket.state == 'rejected'">
                            <hr/>
                            <div class="alert alert-danger">
                                <h5>Rejection Reason:</h5>
                                <p>
                                    <t t-esc="ticket.rejection_reason"/>
                                </p>
                                <p>
                                    <small>Rejected by:
                                        <t t-esc="ticket.rejected_by_id.name"/>
                                        on
                                        <t t-esc="ticket.rejected_date.strftime('%Y-%m-%d %H:%M')"/>
                                    </small>
                                </p>
                            </div>
                        </t>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <!-- No Employee Error -->
    <template id="portal_no_employee" name="No Employee">
        <t t-call="portal.portal_layout">
            <div class="container mt-5">
                <div class="alert alert-warning">
                    <h4>No Employee Record Found</h4>
                    <p>You don't have an employee record linked to your user account. Please contact your
                        administrator.
                    </p>
                </div>
            </div>
        </t>
    </template>

</odoo>